/**
 * Re-export from canonical hook location for consistency.
 * This ensures all components use the same mobile detection logic.
 */
export { useIsMobile } from '../productivity-dashboard/hooks/useIsMobile';
